4.	В модуле Trial создать	модель News (соответственно создать табличку в БД
		news(Id (ключевое поле, автоинкремент), title (заголовок новости 50 символов), content (текст), image (путь к картинке 250 символов)) )
	Создать коллекцию модели. В админ панели создать Grid( таблицу ) и форму редактирования новостей.
	На frontend ( сайт ) создать в IndexController newsListAction ( layout + шаблон ) вывод списка новостей
	(сверху заголовок, слева картинка ресайз до 120 px, справа пару строчек новости) и newsViewAction ( layout + шаблон ) вывод конкретной новости
	( передавать параметр id новости ).